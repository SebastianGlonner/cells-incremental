import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.efesus.cellsincremental',
  appName: 'CellsIncremental',
  webDir: 'build',
  bundledWebRuntime: true
};

export default config;
